const state = {
    isDebugEnabled: true
};

const mutations = {}

const getters = {
    isDebugEnabled: state => state.isDebugEnabled
}

const actions = {}

export default {
    namespaced: true,
    state,
    mutations,
    getters,
    actions
}
